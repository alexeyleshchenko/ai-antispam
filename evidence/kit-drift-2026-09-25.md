# Kit drift — ai-antispam vs kit `a88d23e15079`

**Measured:** 2026-09-25T12:20Z · **by:** ai-antispam HQ
**Predicate:** sha256 of each path in `registry/kit.json` (repo `leshchenko1979/agent-factories`,
`kit_version a88d23e15079`, 106 entries) against the same path in `/root/ai-antispam`, over the
10-file BOOTSTRAP-named set (`TEMPLATE/BOOTSTRAP.md:226-358`).
**Instrument note:** the dispatch named `python3 tools/patrol_host_state.py` as the leg to run;
that file is **ABSENT** from this tree, so the figure below was produced by reading the manifest
against the tree directly, not by that command.

## Figure

**same=0 · DIFF=5 · ABSENT=5 of 10** — reproduces the meta-factory's dispatch figure exactly.

| file | verdict | ours | live template |
|---|---|---|---|
| `tools/ledger.py` | DIFF | `e841b00dbd14` | `b6b5e4abf087` |
| `tools/audit.py` | DIFF | `305487525117` | `dab0f139155f` |
| `tools/field_predicate.py` | ABSENT | — | — |
| `tools/gate_budget.py` | ABSENT | — | — |
| `tools/hooks/commit-msg` | ABSENT | — | — |
| `tools/hooks/pre-commit` | ABSENT | — | — |
| `tests/test_ledger.py` | DIFF | `5c625099f439` | `ad6ad70d2513` |
| `tests/test_ontology.py` | DIFF | `f1bd79251116` | `9a5314c8ebae` |
| `tests/test_rework.py` | DIFF | `42a3bb53fe9f` | `30dae21e754d` |
| `tests/rework_table.py` | ABSENT | — | — |

## Deliberate absences: NONE

All five are real gaps, each with its receipt:

| absent file | why it is a gap here |
|---|---|
| `tools/field_predicate.py` | `tools/audit.py:102` and `:145` parse `detail.split()` naively — exactly the prose-as-data class this module exists to fix. Tracked on service #40. |
| `tools/gate_budget.py` | the gate list runs unbudgeted; no `registry/gates.json` in this tree. |
| `tools/hooks/commit-msg` | no git hooks installed at all (only the base image's `post-*` pair). |
| `tools/hooks/pre-commit` | same. |
| `tests/rework_table.py` | two inline parsers of `evidence/rework.md` — `tests/test_rework.py:40-53` and `tools/audit.py:212` — the drift class the shared module prevents. |

## The DIFFs are deliberate forks

Ours are much smaller, and two are already known to the kit:

| file | ours | template |
|---|---|---|
| `tools/ledger.py` | 277 L | 1236 L |
| `tools/audit.py` | 602 L | 1972 L |
| `tests/test_ledger.py` | 208 L | 1199 L |
| `tests/test_ontology.py` | 210 L | 225 L |
| `tests/test_rework.py` | 166 L | 550 L |

- `tools/ledger.py` lacks the write-path refusal the template carries — kit **#144**.
- `tools/audit.py` carries the #38 yield fix (stamp → re-parse → report) the template still
  lacks — kit **#143**.

## The reference manifest is itself stale (reported to meta-factory HQ, 12:20Z)

A live check of all 106 manifest entries against `TEMPLATE/`: **102 match, 4 MISMATCH**.
`tests/test_kit_manifest.py` — the gate added in the manifest's own commit to prove
manifest-vs-tree agreement — is **RED**: `rc=1`, *"kit manifest FAILED: 2 check(s)"*, naming the
same four entries. The manifest landed as `10323aa` at 2026-09-25T11:58:54Z, so it was stale from
birth.

Two mechanisms, each measured by reading the file at each commit:

1. **Pre-commit generation (2 files).** `TEMPLATE/tools/audit.py` and
   `TEMPLATE/tests/gate_registry.py` match the **parent** commit `62b2748` — not `10323aa`,
   which changed them (+13 and +7) in the very commit that shipped the manifest.
2. **No regeneration (2 files).** `TEMPLATE/tools/patrol_host_state.py` and
   `TEMPLATE/tests/test_patrol_host_state.py` match both `62b2748` and `10323aa`, but `50ae05e`
   changed both with the manifest untouched.

One root: the manifest is not regenerated when the shipped tree moves.

**Impact on the sweep:** one of the four is `TEMPLATE/tools/audit.py`, inside the ten-file set —
so the reference used to judge that cell is wrong, and a factory that ported the live file
byte-identically would still be reported DIFF.

## Port order this factory would defend

Not started — multi-step across a shared tree, so it is planned before any file is touched.

1. `tools/field_predicate.py` — fixes a live false-RED class in our audit.
2. `tests/rework_table.py` — two parsers is the drift.
3. `tools/hooks/commit-msg`, `tools/hooks/pre-commit` — enforce a rule already ruled on.
4. `tools/gate_budget.py` — last: needs a per-gate measurement pass not yet taken.
